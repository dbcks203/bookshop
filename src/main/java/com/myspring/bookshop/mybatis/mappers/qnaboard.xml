<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
           "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.myspring.bookshop.mybatis.mappers.QnaBoardDAO">


	<select id="selectView"
		resultType="com.myspring.bookshop.qnaboard.entity.QnaBoardVO" parameterType="String">
		select
		article_no,
		parent_no,
		book_no,
		article_type,
		title,
		content,
		writeId,
		writeDate,
		viewCount
		from qnaboard
		where article_no = #{article_no}
	</select>
	
	<insert id="insertArticle"
		parameterType="com.myspring.bookshop.qnaboard.entity.QnaBoardVO">
		insert qnaboard(
		parent_no,
		book_no,
		article_type,
		title,
		content,
		writeId)
		values(
		#{parent_no},
		#{book_no},
		#{article_type},
		#{title},
		#{content},
		#{writeId})
	</insert>
	
	<delete id="deleteArticle">
		delete from qnaboard
		where article_no=#{article_no}
	</delete>
</mapper>